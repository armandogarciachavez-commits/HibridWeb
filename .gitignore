# Dependencias y logs
node_modules/
npm-debug.log
yarn-error.log

# Mac
.DS_Store

# Directorios de compilación (si hay en el root)

build/

# Entorno
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDEs
.idea/
.vscode/

# Ignorar archivos comprimidos de backups
*.zip

# Archivos propios del AI
.claude/

# Configuración local del bridge (contiene tokens reales)
biometric-bridge/appsettings.json
biometric-bridge/publish/appsettings.json
backend/app/Http/Controllers/AdminController.php.backup-pre-fingerprint-fix

# Logs de PHP-FPM / Apache (autogenerados en producción)
error_log
**/error_log

# .htaccess autogenerados por cPanel en dist/ de PWAs
pwa-*/dist/.htaccess
